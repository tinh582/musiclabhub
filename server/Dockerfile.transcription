FROM python:3.11-slim

WORKDIR /app

COPY server/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY server/transcription_service.py ./transcription_service.py

ENV HOST=0.0.0.0

CMD ["sh", "-c", "gunicorn -b 0.0.0.0:${PORT:-8000} transcription_service:app"]